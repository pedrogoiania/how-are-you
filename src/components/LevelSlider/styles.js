import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  percentValuesContainer: {
    paddingHorizontal: 25,
  },
  sliderContainer: {
    paddingHorizontal: 10,
  },
  textValuesContainer: {
    paddingHorizontal: 15,
  },
});

export default styles;
