import { StyleSheet } from 'react-native';

import { darkPurple } from '../../../../components/Colors';

const styles = StyleSheet.create({
  buttonContainer: {
    bottom: 20,
    padding: 20,
    position: 'absolute',
    width: '100%',
  },
  feelingLevelContainer: {
    backgroundColor: darkPurple,
    flex: 5,
  },
});

export default styles;
